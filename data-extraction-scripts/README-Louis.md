# ML-Agents Python package

`pip install mlagents==1.1.0`

`pip install tensorboard pandas`

## Unity ML-Agents Toolkit

`git clone https://github.com/andrewViscu/ml-agents-14.git`

`cd ml-agents-14`

`git checkout mlagents-14`

`pip install -e ./ml-agents-envs`

`pip install -e`

### Quick start

``` bash
python ./data-extraction-scripts/data_extraction_script.py <config_file_path> <env_path> <run_id>
```
